# AI Daily News - 全自动工作流
# 每天北京时间16:00自动采集、处理、发布

name: Daily AI News

on:
  schedule:
    - cron: '0 8 * * *'  # UTC 08:00 = 北京时间 16:00
  workflow_dispatch:  # 支持手动触发
  push:
    branches: [main]  # 推送时也触发部署

# 设置权限
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  # 任务1: 采集新闻并更新数据
  collect-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: 🐍 设置Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 安装依赖
        run: pip install -r requirements.txt

      - name: 🚀 采集新闻
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python src/main.py

      - name: 📁 更新docs目录
        run: |
          mkdir -p docs/data/daily
          cp -r data/* docs/data/
          echo "数据已同步到docs目录"
          ls -la docs/data/

      - name: 📤 提交更新
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "无更新"
          else
            git commit -m "📰 自动更新 $(TZ='Asia/Shanghai' date +'%Y-%m-%d %H:%M') AI动态"
            git push
          fi

  # 任务2: 部署到GitHub Pages
  deploy:
    needs: collect-and-build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4
        with:
          ref: main
          
      - name: 🔄 拉取最新更改
        run: git pull origin main

      - name: 📦 配置Pages
        uses: actions/configure-pages@v4

      - name: 📤 上传到Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'docs'

      - name: 🚀 部署Pages
        id: deployment
        uses: actions/deploy-pages@v4
